[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd frontend && npm install"]

[phases.build]
cmds = ["cd frontend && npx prisma generate && next build"]
dependsOn = ["install"]

[start]
cmd = "cd frontend && npx prisma migrate deploy && npm start"
