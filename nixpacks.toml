[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd frontend && npm ci"]

[phases.build]
cmds = ["cd frontend && npx prisma generate && npx next build"]
dependsOn = ["install"]

[start]
cmd = "cd frontend && npx prisma migrate deploy && npx next start"
